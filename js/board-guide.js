/**
 * 페이지별 기능 안내 (정적 콘텐츠, SQL/DB 미사용)
 * 게시판 페이지에서 "페이지별 기능 안내" 섹션으로 렌더링됩니다.
 */
(function () {
  const BOARD_GUIDE_ARTICLES = [
    {
      id: 'input',
      pageName: '성적 입력/관리',
      title: '성적 입력 및 수정 페이지',
      content: `
<h4>1. 기능 개요</h4>
<p>학생 한 명의 성적을 <strong>직접 입력</strong>하거나 <strong>엑셀(XLSX/XLS) 파일로 일괄 업로드</strong>할 수 있는 페이지입니다. 상단의 <strong>직접 입력</strong> 탭과 <strong>엑셀 업로드</strong> 탭으로 구분되어 있으며, 학번을 입력하면 기존 데이터가 있으면 불러와 수정할 수 있고, 없으면 신규 학생으로 저장할 수 있습니다. 재수강 이력이 있는 과목은 이 페이지에서는 수정할 수 없으며, "재수강 성적 입력" 페이지에서만 변경 가능합니다.</p>

<h4>2. 직접 입력 탭 – 입력 항목</h4>
<ul>
  <li><strong>학번</strong>: 필수 입력. 3자 이상 입력 시 자동으로 해당 학번을 검색합니다. 기존 학생이면 저장된 성적이 폼에 채워지고, 없으면 빈 폼으로 신규 입력 상태가 됩니다. 학번은 중복 없이 한 학생당 하나의 행(student_grades)에 대응됩니다.</li>
  <li><strong>졸업 연도</strong>: 선택 입력. 숫자 4자리(예: 2025)로 입력할 수 있으며, 성적 요약·통계에서 연도별 필터링에 사용됩니다.</li>
  <li><strong>레벨 선택</strong>: L1, L2, L3 중 하나를 선택합니다. 선택한 레벨에 해당하는 <strong>18개 핵심술기 과목</strong>(활력징후, 호흡, 순환 등)만 입력란과 표가 표시됩니다. 다른 레벨 데이터가 있어도 현재 선택한 레벨만 저장 시 반영됩니다.</li>
  <li><strong>과목별 입력</strong>: 각 과목마다 <em>점수(0~100, 소수 가능)</em>, <em>연도</em>, <em>과목명</em>, <em>달성도(상/중/하)</em>를 입력합니다. 저장 시 점수·연도·과목명은 <strong>"점수(연도, 과목명)"</strong> 형식으로 DB에 저장되며, 달성도는 "상", "중", "하" 중 하나로 저장됩니다.</li>
  <li><strong>비고</strong>: 자유 텍스트로 해당 학생에 대한 메모를 남길 수 있으며, DB의 비고 컬럼에 그대로 저장됩니다.</li>
</ul>

<h4>3. 직접 입력 탭 – 버튼 동작</h4>
<ul>
  <li><strong>성적 저장</strong>: 버튼 클릭 시 <strong>현재 선택된 레벨(L1/L2/L3)의 입력값만</strong> 반영됩니다. 기존 학생이면 해당 레벨의 해당 과목만 갱신되고, 신규 학생이면 새 행이 생성됩니다. 다른 레벨의 데이터는 변경되지 않습니다.</li>
  <li><strong>초기화</strong>: 폼 내용을 비우고 검색 상태를 해제하여, 새 학번을 입력할 수 있는 상태로 만듭니다.</li>
  <li><strong>학생 정보 삭제</strong>: 기존 학생을 불러온 경우에만 표시됩니다. 클릭 후 확인하면 <strong>해당 학번의 전체 성적 행이 DB에서 삭제</strong>되며, 복구할 수 없으므로 신중히 사용해야 합니다.</li>
</ul>

<h4>4. 재수강 과목 처리</h4>
<p>해당 학번·레벨에서 <strong>재수강 이력이 있는 과목</strong>(점수나 달성도가 "점수(연도, 과목, 이전점수)" 또는 "중(하)" 같은 재수강 형식으로 저장된 경우)은 입력란이 <strong>비활성화(회색)</strong>되어 수정할 수 없습니다. 저장 시에도 재수강 과목은 기존 값이 유지되며, 재수강 성적 변경은 반드시 <strong>"재수강 성적 입력"</strong> 페이지에서만 가능합니다. 이는 재수강 이력을 추적하기 위한 정책입니다.</p>

<h4>5. 엑셀 업로드 탭</h4>
<ul>
  <li><strong>레벨 선택</strong>: 업로드할 성적이 해당하는 레벨(L1/L2/L3)을 선택합니다. 선택한 레벨의 18개 과목에 맞춰 미리보기·저장이 진행됩니다.</li>
  <li><strong>기본 연도·과목명</strong>: 엑셀 시트에 연도·과목명 열이 없을 때 사용할 기본값을 입력할 수 있습니다. 열 매핑 시 해당 필드가 비어 있으면 이 값이 사용됩니다.</li>
  <li><strong>파일 업로드 및 미리보기</strong>: XLSX 또는 XLS 파일을 선택하면 <strong>미리보기 페이지(preview.html)</strong>로 이동합니다. 미리보기에서 <strong>열 매핑</strong>(어떤 열이 학번, 어떤 열이 과목별 점수·달성도인지)을 확인한 뒤 저장하면, 일괄로 student_grades 테이블에 반영됩니다. 학번이 기존에 있으면 해당 레벨만 갱신되고, 없으면 신규 행이 추가됩니다.</li>
</ul>
`
    },
    {
      id: 'view',
      pageName: '성적 조회',
      title: '성적 현황 조회 페이지',
      content: `
<h4>1. 기능 개요</h4>
<p>DB(student_grades)에 저장된 학생 성적을 <strong>학번 검색</strong> 또는 <strong>전체 목록</strong>으로 조회할 수 있는 페이지입니다. 과목별로 표시할 열을 체크박스로 선택·해제할 수 있으며, 학번 검색 시 해당 학생 한 명의 L1·L2·L3 점수와 성취도를 한 화면에서 볼 수 있습니다. 재수강으로 저장된 점수는 빨간색으로 표시되며, 목록에서 <strong>수정</strong> 버튼으로 해당 학생의 성적을 직접 수정할 수 있습니다.</p>

<h4>2. 학번 검색</h4>
<ul>
  <li>상단 검색창에 학번을 입력하고 <strong>검색</strong> 버튼을 누르거나 Enter를 치면, 해당 학번과 <strong>일치하는 학생만</strong> 별도 테이블에 표시됩니다. 이때 전체 목록 테이블은 숨겨지고 검색 결과 테이블만 보입니다.</li>
  <li>검색 결과 테이블에는 <strong>첫 번째 행</strong>: 과목별 L1·L2·L3 <em>점수</em>(맨 앞 숫자만 표시), <strong>두 번째 행</strong>: 과목별 L1·L2·L3 <em>성취도(달성도)</em>가 표시됩니다. 점수 데이터가 "점수(연도, 과목명)" 또는 재수강 형식이어도 화면에는 숫자만 추출되어 표시됩니다.</li>
  <li><strong>재수강으로 저장된 점수</strong>(괄호 안에 쉼표가 2개 이상인 형식)는 <strong>빨간색(retake-score)</strong>으로 표시되어 한눈에 구분할 수 있습니다.</li>
  <li>검색 결과에서 <strong>수정</strong> 버튼을 누르면 페이지 하단에 수정 폼이 열립니다. 폼에서 학번, L1·L2·L3 전체 과목의 점수·달성도·비고를 수정한 뒤 "수정 저장"으로 반영할 수 있습니다. 이 수정은 <strong>일반 형식</strong>(점수(연도, 과목명), 달성도 상/중/하)으로 저장되며, 재수강 형식으로 저장된 항목도 일반 형식으로 덮어쓸 수 있습니다.</li>
</ul>

<h4>3. 전체 목록 보기</h4>
<ul>
  <li>검색창을 비우고 <strong>새로고침</strong>을 누르면 전체 학생 목록이 다시 표시되고, 검색 결과 테이블은 숨겨집니다. 새로고침 시 DB에서 최신 데이터를 다시 불러옵니다.</li>
  <li><strong>레벨 선택</strong>: L1, L2, L3 중 하나를 선택하면 테이블 헤더와 셀에 <strong>해당 레벨의 과목 점수·달성도만</strong> 열로 표시됩니다. 선택한 레벨에 맞춰 18개 과목 열이 구성됩니다.</li>
  <li><strong>과목 선택 체크박스</strong>: 표시할 과목(1_활력징후 ~ 18_배액관 관리)을 개별 선택·해제할 수 있습니다. "전체 선택" / "전체 해제" 버튼으로 한 번에 모두 선택하거나 해제할 수 있어, 필요한 과목만 골라서 보기 편합니다.</li>
</ul>

<h4>4. 수정 폼</h4>
<p>검색 결과에서 <strong>수정</strong> 클릭 시 나타나는 폼에서 <strong>학번</strong>, <strong>L1·L2·L3 각 레벨별 18개 과목의 점수·연도·과목명·달성도</strong>, <strong>비고</strong>를 수정한 뒤 "수정 저장"으로 반영할 수 있습니다. 저장 시 점수는 "점수(연도, 과목명)" 형식으로 저장되며, 달성도는 "상", "중", "하" 중 하나로 저장됩니다. 재수강 형식이었던 항목도 이 수정으로 일반 형식으로 바뀝니다.</p>
`
    },
    {
      id: 'summary',
      pageName: '성적 요약',
      title: '성적 통계 요약 페이지',
      content: `
<h4>1. 기능 개요</h4>
<p>학생 성적을 <strong>졸업 연도별</strong>로 필터링하여, 레벨(L1·L2·L3)별·과목별로 달성도 <strong>상·중·하</strong> 인원 수를 테이블로 확인할 수 있는 통계 페이지입니다. DB(student_grades)의 점수·달성도 데이터를 파싱하여, "점수(연도, 과목명)" 또는 재수강 형식으로 저장된 경우에도 연도·과목별로 그룹핑한 뒤 각 그룹의 상/중/하 인원을 집계합니다.</p>

<h4>2. 사용 방법</h4>
<ul>
  <li><strong>졸업 연도 선택</strong>: 상단 드롭다운에서 "전체" 또는 특정 연도(예: 2024, 2025)를 선택합니다. "전체"를 선택하면 DB에 있는 모든 학생이 집계에 포함되고, 특정 연도를 선택하면 <strong>graduate_year</strong>가 해당 연도인 학생만 집계에 포함됩니다. DB에 저장된 졸업 연도만 드롭다운 옵션으로 표시됩니다.</li>
  <li><strong>새로고침</strong>: 버튼을 누르면 DB에서 최신 데이터를 다시 불러와 통계를 갱신합니다. 졸업 연도를 변경한 뒤에도 자동으로 재집계됩니다.</li>
</ul>

<h4>3. 테이블 구성</h4>
<ul>
  <li>L1, L2, L3 각각에 대해 <strong>별도 테이블</strong>이 있으며, 각 테이블은 해당 레벨의 18개 핵심술기 과목에 대한 통계를 보여줍니다.</li>
  <li>각 테이블에는 <strong>단계(레벨), 과목명, 과목 번호, 연도</strong>와 함께 해당 연도·과목 조합에 대한 <strong>상 / 중 / 하</strong> 인원 수가 열로 표시됩니다. 점수 데이터에서 파싱된 연도·과목명 조합별로 한 행이 만들어지며, 해당 조합에 성적이 있는 학생들 중 달성도가 "상", "중", "하"인 인원이 각각 집계됩니다.</li>
  <li>점수·달성도가 "점수(연도, 과목명)" 또는 재수강 형식(예: "중(하)")으로 저장된 경우, 괄호 안의 연도·과목명·괄호 앞의 달성도가 파싱되어 연도별·과목별로 그룹핑된 뒤, 각 그룹의 달성도 분포(상·중·하)가 인원 수로 집계됩니다.</li>
</ul>
`
    },
    {
      id: 'manage',
      pageName: '관리 필요',
      title: '관리 필요 학생 파악 페이지',
      content: `
<h4>1. 기능 개요</h4>
<p>달성도가 <strong>하</strong>인 학생과, 달성도가 <strong>중</strong> 또는 <strong>하</strong>인 학생을 레벨별로 따로 조회할 수 있는 페이지입니다. 학과에서 추가 지도·관리가 필요한 학생을 빠르게 파악하기 위한 목적이며, 두 개의 테이블(달성도 '하'가 있는 학생 / 달성도 '중'이 있는 학생)이 세로로 배치되어 있습니다. 재수강으로 성적이 변경된 경우에도, <strong>현재 달성도</strong>(괄호 앞 값)가 하 또는 중이면 각각 해당 테이블에 포함됩니다.</p>

<h4>2. 달성도 '하'가 있는 학생</h4>
<ul>
  <li><strong>레벨 선택</strong>: L1, L2, L3 중 하나를 선택합니다. 선택한 레벨에 대해서만 필터링됩니다.</li>
  <li>선택한 레벨에서 <strong>현재 달성도가 '하'인 과목이 하나라도 있는 학생</strong>만 이 테이블에 나열됩니다. 18개 과목 중 한 과목이라도 '하'이면 해당 학생이 포함됩니다.</li>
  <li>저장 형식이 "중(하)"처럼 재수강 형식이어도, <strong>괄호 앞의 값</strong>이 현재 달성도로 간주되므로, "중(하)"는 '중'으로 취급되어 이 '하' 테이블에는 포함되지 않고 '중' 테이블에 포함됩니다. "하" 또는 "하(이전값)" 형태일 때만 이 테이블에 포함됩니다.</li>
  <li>테이블의 각 셀에는 해당 레벨·과목의 달성도 값이 그대로 표시되며(예: 하, 중(하)), <strong>현재 달성도가 '하'인 셀</strong>에는 연한 주황색 배경(low-achievement-cell)이 적용되어 한눈에 구분할 수 있습니다.</li>
</ul>

<h4>3. 달성도 '중'이 있는 학생</h4>
<ul>
  <li>동일하게 상단에서 레벨(L1/L2/L3)을 선택합니다.</li>
  <li>선택한 레벨에서 <strong>현재 달성도가 '중' 또는 '하'인 과목이 하나라도 있는 학생</strong>이 이 테이블에 나열됩니다. 즉 '하'만 있는 학생도 포함되고, '중'이 있는 학생도 포함됩니다.</li>
  <li>재수강 형식 "중(하)", "상(하)" 등은 괄호 앞이 현재 달성도이므로, "중(하)"는 '중'으로 간주되어 이 목록에 포함됩니다. '중' 또는 '하'인 셀에는 연한 주황색 배경이 적용됩니다.</li>
</ul>

<h4>4. 테이블 구성</h4>
<p>두 테이블 모두 헤더는 <strong>학번</strong>과 18개 핵심술기 과목명(1_활력징후 ~ 18_배액관 관리)이며, 각 셀에는 해당 레벨·과목의 달성도 값(상/중/하 또는 재수강 형식)이 표시됩니다. 페이지 로드 시 DB에서 전체 student_grades를 불러온 뒤, 선택한 레벨 기준으로 필터링하여 두 테이블을 각각 렌더링합니다.</p>
`
    },
    {
      id: 'retake',
      pageName: '재수강 성적 입력',
      title: '재수강 성적 입력 페이지',
      content: `
<h4>1. 기능 개요</h4>
<p>이미 성적이 DB에 있는 학생이 <strong>재수강</strong>으로 점수·달성도를 변경했을 때, 그 내용을 반영하는 전용 페이지입니다. "성적 입력/관리" 페이지에서는 재수강 이력이 있는 과목은 수정할 수 없도록 잠겨 있으므로, 재수강 성적 변경은 <strong>이 페이지에서만</strong> 가능합니다. 학번으로 검색한 뒤, 해당 학생이 있을 때만 L1·L2·L3별 18개 핵심술기 과목의 점수·성취도를 테이블로 보고, 행을 클릭해 모달에서 재수강 내용을 입력·저장합니다.</p>

<h4>2. 사용 흐름</h4>
<ul>
  <li><strong>학번 입력</strong>: 검색창에 학번을 입력하고 검색 버튼을 누릅니다.</li>
  <li>해당 학번이 DB(student_grades)에 <strong>있으면</strong>: L1, L2, L3 각각에 대해 18개 핵심술기 과목의 <strong>점수</strong>(맨 앞 숫자만 표시)와 <strong>성취도(달성도)</strong>가 세 개의 테이블로 표시됩니다. 각 과목 행을 클릭하면 모달이 열립니다.</li>
  <li>해당 학번이 <strong>없으면</strong>: "해당 학번의 학생이 없습니다" 메시지만 표시되고, 테이블은 나타나지 않습니다. 먼저 "성적 입력/관리"에서 해당 학번으로 신규 입력해야 합니다.</li>
</ul>

<h4>3. 재수강 입력 방법</h4>
<ul>
  <li>표시된 테이블에서 <strong>수정하고 싶은 과목의 행을 클릭</strong>하면 모달(팝업)이 열립니다. 기존 값이 있으면 모달에 미리 채워집니다.</li>
  <li>모달에서 <strong>수강 년도</strong>, <strong>수강 과목(과목명)</strong>, <strong>점수</strong>, <strong>성취도(상/중/하)</strong>를 입력한 뒤 <strong>저장</strong>합니다.</li>
  <li>저장 시 <strong>기존 값이 있으면</strong>: 점수는 <strong>"새점수(수강 년도, 수강 과목, 이전 점수)"</strong> 형식으로, 성취도는 <strong>"새성취도(이전 성취도)"</strong> 형식(예: 중(하))으로 DB에 저장되어 재수강 이력이 남습니다. <strong>이전 값이 없으면</strong>: 점수는 "점수(연도, 과목)", 성취도는 "상/중/하"만 저장됩니다.</li>
</ul>

<h4>4. 데이터 형식 정리</h4>
<ul>
  <li><strong>최초 입력 또는 성적 조회 수정</strong>: 점수 → "점수(연도, 과목명)", 성취도 → "상", "중", "하" 중 하나.</li>
  <li><strong>재수강 페이지에서 수정</strong>: 점수 → "점수(수강 년도, 수강 과목, 이전 점수)" (괄호 안에 쉼표 2개 이상), 성취도 → "새성취도(이전 성취도)" (예: 중(하)). 이 형식으로 저장된 과목은 "성적 입력/관리"에서 잠기고, "재수강 인원 조회"에서 재수강 이력으로 표시됩니다.</li>
</ul>
`
    },
    {
      id: 'retake-list',
      pageName: '재수강 인원 조회',
      title: '재수강 인원 조회 페이지',
      content: `
<h4>1. 기능 개요</h4>
<p>재수강 이력이 <strong>한 건이라도 있는 학생</strong>만 모아서, 레벨별로 과목당 <strong>"이전 점수(이전 달성도) → 현재 점수(현재 달성도)"</strong> 형태로 조회할 수 있는 페이지입니다. "재수강 성적 입력" 페이지에서 저장한 형식(점수: 괄호 안에 쉼표 2개 이상, 달성도: 괄호 포함)이 있는 학생만 목록에 나타나며, 재수강이 전혀 없는 학생은 테이블에 나오지 않습니다.</p>

<h4>2. 사용 방법</h4>
<ul>
  <li><strong>레벨 선택</strong>: L1, L2, L3 중 하나를 선택합니다. 선택한 레벨에 대해서만 재수강 데이터가 있는 학생이 필터링되어 테이블에 표시됩니다.</li>
  <li>선택한 레벨에서 <strong>재수강 데이터가 한 건이라도 있는 학생</strong>만 테이블에 나열됩니다. 해당 레벨에 재수강이 없는 학생은 목록에 포함되지 않습니다.</li>
</ul>

<h4>3. 테이블 구성</h4>
<ul>
  <li>헤더: <strong>학번</strong> + 18개 핵심술기 과목명(1_활력징후 ~ 18_배액관 관리).</li>
  <li>각 셀 내용:
    <ul>
      <li><strong>재수강이 있는 과목</strong>: "이전점수(이전달성도) → 현재점수(현재달성도)" 형태로 표시됩니다 (예: 5(하) → 9(상)). <strong>현재 부분</strong>(화살표 오른쪽, 예: 9(상))은 <strong>빨간색</strong>으로 표시되어 재수강으로 변경된 값을 한눈에 구분할 수 있습니다.</li>
      <li><strong>재수강이 없는 과목</strong>: "현재점수(현재달성도)" 또는 "-"만 표시됩니다. 재수강 형식이 아닌 일반 형식으로만 저장된 경우 이렇게 표시됩니다.</li>
    </ul>
  </li>
</ul>

<h4>4. 재수강 판별 기준</h4>
<p>점수가 <strong>"점수(수강 년도, 수강 과목, 이전 점수)"</strong> 형식(괄호 안에 쉼표가 2개 이상)이거나, 달성도가 <strong>"새성취도(이전 성취도)"</strong> 형식(괄호 포함, 예: 중(하))이면 재수강으로 간주됩니다. "재수강 성적 입력" 페이지에서 저장한 데이터가 이 형식으로 저장되며, "성적 입력/관리" 페이지에서는 해당 과목이 잠겨 수정할 수 없게 됩니다.</p>
`
    },
    {
      id: 'board',
      pageName: '게시판',
      title: '공지 및 안내 게시판 페이지',
      content: `
<h4>1. 기능 개요</h4>
<p>학과 공지나 프로그램 사용 안내 등을 게시하는 게시판입니다. DB(announcements 테이블)에 저장된 게시글을 목록으로 조회하고, 검색·새 글 작성·글 상세 보기(조회수 증가)·삭제가 가능합니다. 상단 탭으로 <strong>페이지별 기능 안내</strong>와 <strong>게시글 보기</strong>를 전환할 수 있으며, 게시글 보기 탭에서 목록·검색·페이지 이동·새 글 작성이 이루어집니다.</p>

<h4>2. 목록 보기</h4>
<ul>
  <li>페이지 로드 시 DB(announcements)에서 게시글을 <strong>최신순(created_at 내림차순)</strong>으로 불러와 테이블에 표시합니다. 고정글(is_pinned=true)이 있으면 목록 상단에 먼저 배치됩니다.</li>
  <li>테이블에는 <strong>번호, 제목, 작성자, 작성일, 조회수</strong>가 표시됩니다. 제목을 클릭하면 상세 페이지로 이동합니다.</li>
  <li>한 페이지에 <strong>10건씩</strong> 표시되며, 하단의 <strong>이전 / 다음</strong> 버튼으로 페이지를 넘길 수 있습니다. 검색 결과가 있을 때는 검색된 글만 페이지네이션됩니다.</li>
</ul>

<h4>3. 검색</h4>
<ul>
  <li>상단 검색창에 키워드를 입력한 뒤 <strong>검색</strong> 버튼을 누르거나 Enter를 치면, <strong>제목</strong> 또는 <strong>작성자</strong>에 해당 키워드가 <strong>포함된</strong> 글만 목록에 표시됩니다. 대소문자 구분 없이 필터링됩니다.</li>
  <li><strong>새로고침</strong> 버튼을 누르면 검색창이 비워지고 전체 목록이 다시 로드됩니다.</li>
</ul>

<h4>4. 새 글 작성</h4>
<ul>
  <li><strong>새 글 작성</strong> 버튼을 누르면 글 작성 페이지(board-write.html)로 이동합니다.</li>
  <li>작성 페이지에서 <strong>제목</strong>, <strong>작성자</strong>, <strong>내용</strong>을 입력한 뒤 저장하면 announcements 테이블에 새 행이 추가되고, 게시판 목록에 새 글이 표시됩니다. 작성일·조회수는 자동으로 설정됩니다.</li>
</ul>

<h4>5. 글 상세 보기</h4>
<ul>
  <li>목록에서 <strong>제목</strong>을 클릭하면 해당 글의 상세 페이지(board-detail.html)로 이동합니다. URL에 글 ID가 포함됩니다.</li>
  <li>상세 조회 시 해당 글의 <strong>조회수(view_count)</strong>가 1 증가하며, 제목·작성자·작성일·내용이 표시됩니다. 상세 페이지에서 <strong>삭제</strong> 버튼을 누르면 확인 후 해당 글이 DB에서 삭제되고 목록 페이지로 돌아갈 수 있습니다.</li>
</ul>

<h4>6. 페이지별 기능 안내</h4>
<p>이 게시판 페이지의 <strong>페이지별 기능 안내</strong> 탭에는, <strong>가입 요청 관리를 제외한</strong> 모든 페이지(성적 입력/관리, 성적 조회, 성적 요약, 관리 필요, 재수강 성적 입력, 재수강 인원 조회, 게시판)에 대한 상세한 사용 방법이 정리되어 있습니다. 각 항목을 펼치면 해당 페이지의 기능·입력 항목·버튼 동작·데이터 형식 등을 자세히 확인할 수 있으며, 가입 요청 관리(관리자 전용)는 별도 운영 안내에 따릅니다.</p>
`
    }
  ];

  function renderGuides() {
    const container = document.getElementById('board-guide-list');
    if (!container) return;

    container.innerHTML = BOARD_GUIDE_ARTICLES.map(function (article) {
      return (
        '<details class="board-guide-item">' +
        '<summary class="board-guide-summary">' +
        '<strong>' + escapeHtml(article.pageName) + '</strong> – ' + escapeHtml(article.title) +
        '</summary>' +
        '<div class="board-guide-content">' + article.content + '</div>' +
        '</details>'
      );
    }).join('');
  }

  function escapeHtml(text) {
    if (!text) return '';
    const div = document.createElement('div');
    div.textContent = text;
    return div.innerHTML;
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', renderGuides);
  } else {
    renderGuides();
  }
})();
